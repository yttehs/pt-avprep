# Step 4 (face detection/tracking) and Step 4b (face clustering into persistent
# identities). Pulls in TensorFlow via MTCNN and keras-facenet -- keep this in
# its own venv, don't mix with the torch-based requirements below.
# Recommended Python: 3.10 or 3.11 (broadest tensorflow wheel availability).
opencv-python>=4.13.0
numpy
mtcnn>=1.0.0
tensorflow-cpu>=2.21.0
keras-facenet>=0.3.2
scipy
matplotlib
